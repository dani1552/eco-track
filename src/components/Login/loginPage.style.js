import styled from "styled-components";

export const Title = styled.input`
  width: 100%;
  color: root(--color-darkgray);
  font-weight: root(--weight-semi-bold);
  font-size: root(--font-medium);
`;
